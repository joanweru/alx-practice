#include <stdio.h>
/**
 * main - print if else time
 * Return : return 0 if code was success
 */
int main(void)
{
	int num = 15;

	if (num > 25)

	{
		printf("tru");
	}
	else
	{
		printf("no");
	}
	return (0);
}
